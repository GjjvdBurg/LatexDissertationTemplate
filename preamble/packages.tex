\usepackage[utf8]{inputenc}

% math
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amsthm}
\usepackage{mathtools}

% language
\usepackage[dutch,english]{babel}
\usepackage{xspace}
\usepackage{textcomp}

% colors
\usepackage[pdftex, dvipsnames]{xcolor}

% figures
\usepackage[center]{subfigure}
\usepackage{graphicx}
\usepackage{wrapfig}

% tables
\usepackage[figuresright]{rotating}

% fonts
\usepackage{fouriernc} % Fourier math with NC Schoolbook
\usepackage{BOONDOX-cal} % Boondox calligraphy for \mathcal
\usepackage[T1]{fontenc}
\usepackage{microtype}
\usepackage{setspace}

% section title and toc
\usepackage[explicit,newparttoc,rigidchapters]{titlesec}
\usepackage[dotinlabels]{titletoc}

% appendix
\usepackage[toc]{appendix}

% algorithms
\usepackage[chapter]{algorithm}
\usepackage{algpseudocode}
\usepackage{etoolbox}
% use normal text in algorithms instead of bold
\AtBeginEnvironment{algorithmic}{\let\textbf\relax}

% captions
\usepackage[%
	margin=10pt,
	font={},
	textfont={it},
	labelfont={color=eurgreen, sc},
	labelsep=endash]{caption}
\captionsetup[algorithm]{labelsep=period, labelfont=rm}

% references
\usepackage{natbib}
\usepackage[hidelinks]{hyperref}
\usepackage{bookmark}

% pagestyle
%\newcommand*\GetTextWidth[3][\normalfont\normalsize]{{#1%
%		\settowidth{#2}{abcdefghijklmnopqrstuvwxyz}%
%		\setlength{#2}{0.03193#2}%
%		\addtolength{#2}{0.44961pt}%
%		\setlength{#2}{#3#2}%
%		\global#2=#2}}
%\newlength\bringhurstwdt
%\GetTextWidth{\bringhurstwdt}{66}

% official
%\usepackage[%
%	paperwidth=165mm,
%	paperheight=240mm,
%	inner=18mm, outer=18mm,
%	bottom=24mm, top=24mm
%	]{geometry}

% compromise?
\usepackage[%
	paperwidth=165mm,
	paperheight=240mm,
	inner=25mm,
	top=20mm,
	outer=20mm,
	bottom=28mm,
	footskip=14mm
	]{geometry}

\special{papersize=165mm,240mm}
\usepackage{afterpage}
\usepackage{emptypage}

% enumerations
\usepackage{enumerate}
\usepackage[shortlabels]{enumitem}

% headers
\usepackage{fancyhdr}

% only for template, remove in your version
\usepackage{blindtext}
